<%= render @chats %>
<%= form_for Chat.new, remote: true do |f| %>
    <%= f.label :chat %><br>
    <%= f.text_area :chat %><br>
    <%= f.hidden_field :recid, :value => @user.id %>
    <%= f.submit "Send" %>
<% end %>